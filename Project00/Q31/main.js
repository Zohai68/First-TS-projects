var users = ["admin", "waqas", "ashraf", "mubeen", "asghar"];
if (users.length == 0) {
    console.log("We need to find some users!");
}
else {
    users = [];
    console.log("All users are removed");
}
